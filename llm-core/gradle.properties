POM_ARTIFACT_ID=skainet-transformers-core
POM_NAME=skainet transformers core

kotlin.mpp.applyDefaultHierarchyTemplate=false
# No androidNative here -- this module wants androidNativeArm32 only (not the
# androidNativeArm64 the plugin's androidNative group would also add), so that target is
# declared manually in build.gradle.kts instead. See that file's own comment.
skainet.targets=jvm,js,wasmJs,wasmWasi,apple,linux

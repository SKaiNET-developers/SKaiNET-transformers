# BitNet-b1.58-2B-4T greedy parity fixture (transformers#360)
#
# Oracle: microsoft/BitNet (bitnet.cpp) @ de371b7 with its pinned llama.cpp fork @ 5eb47b721,
# built with -DBITNET_ARM_TL1=OFF (i2_s CPU path), queried via llama-server on the official
# I2_S GGUF (microsoft/BitNet-b1.58-2B-4T-gguf, ggml-model-i2_s.gguf):
#   build/bin/llama-server -m BitNet-b1.58-2B-4T-i2s.gguf -ngl 0 -t 4 --port 8811
#   curl :8811/tokenize   -d '{"content": "The capital of France is", "add_special": true}'
#   curl :8811/completion -d '{"prompt": "The capital of France is", "n_predict": 32, "temperature": 0, "top_k": 1}'
# Arbitration reference: microsoft/bitnet-b1.58-2B-4T via HF transformers (BF16 activations,
# greedy) — produces the identical 32-token continuation.
#
# History (2026-08-31): the first recording of this fixture diverged from the oracle at
# generated token 4 — the BF16 arbitration sided with the oracle, and the culprit was
# SKaiNET's RoPE pairing (INTERLEAVED instead of the NEOX-style SPLIT_HALF that bitnet.cpp
# assigns all three BITNET arches). With that fixed, SKaiNET (default planes lm_head),
# bitnet.cpp, and the BF16 reference agree token-for-token on all 32 greedy tokens below.
# The exact dense-F16 tied head flips one reference-side near-tie at token 18
# (' also' 0.271 vs ' a' 0.211) — recorded here so a future dense-head golden isn't mistaken
# for a regression.
prompt=The capital of France is
steps=32
prompt_tokens=128000,791,6864,315,9822,374
oracle_text= Paris. Paris is a city that is known for its rich history, culture, and architecture. It is also a major center for art, fashion, and cuisine
skainet_text= Paris. Paris is a city that is known for its rich history, culture, and architecture. It is also a major center for art, fashion, and cuisine
